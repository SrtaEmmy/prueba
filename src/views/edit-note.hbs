<div class="col-md-4 mx-auto">
    <div class="card">
        <div class="card-header">
            <h3>Editar</h3>
        </div>
        <div class="card-body"> <!-- el "?" significa consulta, y luego le indicamos que ser'a mediante el m'etodo PUT -->
            <form action="/edit-note/{{note._id}}?_method=PUT" method="post"> <!-- esta direcci'on es a la que se env'ia al pulsar el bot'on -->
                <input type="hidden" name="_method" value="PUT"> <!-- este m'etodo put es para que el servidor sepa que vamos a enviar mediante el m'etodo "put" -->
                <div class="form-group">
                    <input type="text" name="title" class="form-control" placeholder="Title.." value="{{note.title}}"> <!-- le decimos que antes de editar, se mantenga los datos como los tiene(para que vea lo que tiene mal), y le decimos que del objeto que nos est'a pasando el servidor(note) queremos solo la propiedad "title" -->
                </div>
                <div class="form-group">
                    <textarea name="description" class="form-control">{{note.description}}</textarea> <!-- del objeto que nos envia el servidor(note) solo quiero la propiedad "description" -->
                </div>
                <div class="form-group">
                    <button class="btn btn-primary btn-block" type="submit">Save</button>
                </div>
            </form>
        </div>
    </div>
</div>